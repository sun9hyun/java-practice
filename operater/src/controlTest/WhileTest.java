package controlTest;

public class WhileTest {
   public static void main(String[] args) {
      
      int count = 0; // count 변수 선언
      
      while(count != 10) { // count가  10이 아닐때까지
         count++; // 1씩 증가 
         System.out.println(count + ".한동석");
      }
   }
}